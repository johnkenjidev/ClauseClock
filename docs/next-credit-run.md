# Next Emergent-credit run — pre-committed priority

If the live public voter/judge path exposes a broken or misleading experience, spend the first available credits fixing that path before adding features.

Current static-code priority: refresh the public synthetic demo so `/demo/actions` actually demonstrates the Action Center and the demo reflects already-built finding types.

Do not spend the first run on a new feature unless the public path is already correct in the live deployment.
